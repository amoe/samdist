const getters = {
    age(state, getters) {
        return state.age;
    },
    field(state, getters) {
        return state.field;
    },
    cutoff(state, getters) {
        return state.cutoff;
    }
};

export default getters;
